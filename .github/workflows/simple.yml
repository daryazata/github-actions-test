name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: eccho "hello yaml"
      - name: multiple script
        run: |
          node -v
          npm -v
